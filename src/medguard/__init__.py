"""MedGuard — a medication-safety agent (teaching artifact).

This is the running example for the book *Shipping AI Agents to Production*.
It is **not** a medical device and must never be used for real clinical
decisions.

Version arc: this package starts at the book's ``v0`` — a deliberately naive,
single-model-call agent with no evaluation, grounding, tracing, or guardrails.
Later chapters (and git tags ``v1``..``v5``) harden it toward production.
"""

__version__ = "0.0.0"  # v0
