"""Thin ports around canonical MedGuard. Not a second product.

The v0–v5.2 agent lives in ``src/medguard/``. This package is one runnable
slice (tools + decide, then the independent dose-ceiling veto) plus adapters
and framework wrappers. See ``ports/README.md``.
"""
